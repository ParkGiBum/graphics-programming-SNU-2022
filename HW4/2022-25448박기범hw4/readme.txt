상당수 코드를 pdf 링크에 있는 코드를 참고 하였습니다
강아지 모델을 추가 하였습니다 (gun)

pcf의 경우에는 링크에 나온 4개짜리 푸아송 테이블을 이용하여 랜덤제네레이터%4의 인덱스에 해당하는 주변 좌표값들의 그림자 값들을 평균 내서 구현 하였습니다. 이를 키기 위해서는 4번을 키게 되면 shadow 값들이 샘플링 대서 구현대게 됩니다.
샘플링 횟수는 임의로 25번으로 잡았습니다.

csm의 경우 링크에 나온 코드를 상당 부분 참고 하였습니다. csm의 경우 여러 종류의 바이아스를 따로 계산 하여 정확한 바이아스를 구현 하는 것이 목표 이므로 가장 멀때 중간 가까울때 세가지 종류로 나누어서 bias를 계산 하여야 합니다. 이를 위하여 getLightSpaceMatrices함수에서 거리에 따라서 캐스케이드 레벨을 정해 주게 됩니다. 이 계산을 위해서는 getLightSpaceMatrix에서 프로젝션 매트릭스와 뷰 매트릭스를 거리에 따라 다시 계산하여 줍니다. 이제 이 값을 쉐이더로 넘겨주게 되면 쉐이더에서 거리에 따라서 다른 lightSpaceMatrices를 적용하여 그림자를 구현하게 됩니다.